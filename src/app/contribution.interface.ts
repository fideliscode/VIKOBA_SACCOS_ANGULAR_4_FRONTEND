export interface Contribution{

            id: number;
            user_id: number;
            group_id: number;
            amount: number;
            created_at: string;
            updated_at: string;

}
